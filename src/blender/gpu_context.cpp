#include "blender/gpu_context.h"

namespace sep::blender {

// All GPUContext functionality is defined inline in the header. This
// translation unit exists only to satisfy legacy build systems and is now
// intentionally empty.

}  // namespace sep