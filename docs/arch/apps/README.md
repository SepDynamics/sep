# `src/apps`

This directory holds standalone applications built on top of the SEP Engine. The programs here demonstrate or run the engine in specific contexts. `data_downloader.cpp` provides simple utilities for pulling market data. The `oanda_trader` subfolder contains a full graphical interface for live trading using OANDA data. Common UI helpers reside under `common`.
