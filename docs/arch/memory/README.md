# `src/memory`

Implements the tiered memory system, unified memory helpers, and persistence utilities. The code here manages hot/cold data through STM/MTM/LTM layers and interacts with external storage (e.g., Redis) to maintain pattern data.
