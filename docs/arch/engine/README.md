# `src/engine`

Core computational engine for pattern analysis and trading logic. This directory includes CUDA kernels, event processing, managers, and general utilities that operate at high performance. It is the heart of the SEP system where patterns are parsed, metrics generated, and signals emitted.
