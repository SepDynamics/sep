[Unit]
Description=MCP Filesystem Server
After=network.target

[Service]
Type=simple
User=ajn
WorkingDirectory=/sep
ExecStart=/usr/bin/npx -y @modelcontextprotocol/server-filesystem /sep
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target